{
  "name": "@motherlabs/ada",
  "version": "0.1.12",
  "description": "Ada — semantic compiler. Turn intent into governed Claude Code sessions.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "ada": "dist/index.js"
  },
  "files": [
    "dist/",
    "README.md"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsup",
    "build:types": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no tests yet'",
    "prepublishOnly": "pnpm run build"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.80.0",
    "ink": "^5.2.0",
    "react": "^18.3.1"
  },
  "optionalDependencies": {
    "better-sqlite3": "^9.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "@types/react": "^18.3.0",
    "tsup": "^8.5.1",
    "typescript": "^5.7.0"
  }
}
