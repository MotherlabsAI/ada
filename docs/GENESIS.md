# GENESIS.md — The Motherlabs AIgency & Semantic OS

**Authority:** Root specification for the Motherlabs AIgency and the underlying Governed Semantic Runtime (GSR).
**Architect:** Alex
**Status:** Compiled & Frozen (Fixed Point reached 2026-03-31)

---

## 1. System Brief
The Motherlabs AIgency is a **Self-Sustaining Dissipative System** and **Governed Semantic Runtime** designed for autonomous economic extraction. It operates as a local-first semantic operating system that compiles natural language intent into a low-entropy, multi-dimensional **Symmetric Contextual Cayley Graph** (the World Model). This manifold is the primary source of truth, from which application code and revenue-generating agents are projected. The system enforces a synchronous, axiomatic write-barrier (The Governor) to prevent semantic drift, ensuring that all autonomous actions—from code generation to capital expenditure—are mathematically reachable from the architect's core intent.

---

## 2. Core Architectural Philosophy: The Rubik's Manifold
This system rejects "probabilistic slop" in favor of **Structural Realism**. 
- **The Cayley Graph:** The codebase and state are a high-dimensional state-space.
- **Atomic Permutations:** Every action (twist) is a transformation that must maintain system symmetry.
- **God's Number:** A threshold for maximum permissible logical depth (drift) from core intent.
- **The Inversion Principle:** Every state change must be traceable and reversible to its justifying axiom.

---

## 3. System Ontology
- **Intent:** High-entropy natural language root input.
- **Axiom:** Immutable structural law (e.g., Distinction, Composition, Entropy Monotonicity).
- **Invariant:** Domain-specific constraint derived from Axioms (e.g., "Revenue > Expense").
- **Postcode:** 5-axis coordinate (`Layer.Concern.Scope.Dimension.Domain`) for semantic addressing.
- **Fixed Point:** The "Solved State" where all invariants are satisfied.
- **Governor:** Synchronous write-barrier intercepting all tool-calls and state transitions.
- **Residue:** Unresolved semantic gates requiring human (Alex) disambiguation.

---

## 4. The AIgency: Functional Goals
- **Autonomy:** MAPE-K architecture (Monitor, Analyze, Plan, Execute, Knowledge).
- **Economic Loop:** Utility function bound to verified capital inflow.
- **Capital Actuation:** Autonomous ability to provision compute and hardware within risk boundaries.
- **Role Inversion:** The AIgency is the Macro-Planner; the Human (Alex) is the physical actuator and capital sink.

---

## 5. Genesis Invariants (The Fixed Centers)
1. **The Autopoiesis Invariant:** Value In > Value Out. No capital expenditure without projected ROI > Governor Threshold.
2. **The Sovereignty Invariant:** The World Model and Governance Core must reside on hardware physically owned by Alex.
3. **The Asymmetric Trust Boundary:** Total digital autonomy; manual "Human Gate" for physical/financial actions > $X.
4. **Entropy Monotonicity:** Every compilation stage and execution turn must decrease or maintain system entropy.

---

## 6. Runtime Architecture (GSR-V1)
1. **Ingress:** NL Parser & Uncertainty Classifier.
2. **Compiler:** NL-to-IR (Intermediate Representation) mapping via the 9-stage pipeline.
3. **World Model (Graph Store):** Git-backed content-addressed object store.
4. **Verifier:** Pure-function Axiom evaluator.
5. **Governor:** Synchronous Write-Barrier (Tool Interceptor).
6. **Planner/Executor:** Autonomous agent loop for reaching Fixed Points.
7. **Emitter:** Deterministic code projector (World Model -> Code).
8. **Repo Adapter:** Manages `.ada/ref` and `.git/objects`.

---

## 7. Storage and Truth Model
- **Source of Truth:** The IR and the content-addressed World Model (Cayley Graph).
- **Derived Artifacts:** All source code (`.ts`, `.js`, etc.). Code is disposable and re-projectable.
- **Persistence:** Immutable, append-only git objects.
- **Pointer:** `.ada/ref` stores the current "Solved State" SHA.

---

## 8. Implementation Strategy
- **Codebase:** TypeScript Monorepo (pnpm workspaces).
- **Interception:** Sync hooks in the `QueryEngine` to prevent drift.
- **Verification:** 5-layer stack (Type, Axiom, Brick, Resource, Governance).

---

## 9. Initial Repository Blueprint (Top 5 Milestones)
1. **M01 — Core IR:** Define the 5-axis Postcode and PostcodeAddress schemas.
2. **M02 — Git Store:** Implement the `git hash-object` based storage and `.ada/ref` pointer.
3. **M03 — The Governor:** Build the synchronous tool-call interceptor for Gemini/OpenClaw.
4. **M04 — The Pipeline:** Implement the 9-stage CTX-GOV compiler engine.
5. **M05 — Emitter:** Build the first deterministic projector for `CLAUDE.md` and agent MD files.

---

## 10. Alex's Sovereign Commands
- *Point the direction; the AIgency grows.*
- *The AIgency scales; the Human enjoys.*
- *Axioms are Law; Code is Projection.*
