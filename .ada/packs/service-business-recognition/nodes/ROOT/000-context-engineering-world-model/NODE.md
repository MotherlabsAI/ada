# ◇ ● ∴ ⇒ ROOT.000 — Context Engineering World Model

- cluster: ROOT · depth: L5 · truth: ∴ inference
- checkability: C0 (uncheckable)
- compiles to: graph, wiki, claude, blueprint

**Summary.** The working world model compiled from one intent. It does not model everything — only the bounded context needed to act: ATT, COPY, SEO, UNK.

**Why.** The map the executor operates inside. Without it, Claude Code builds from a raw prompt.

**Failure if missing.** The build proceeds on guesses instead of structure.

See `wiki.md` for the full article, `edges.yaml` for links, `checkability.yaml` for checks.
