# FLOW.063 · Reader — full-width capsule + neighbour strip

> ∴ inference · L3 · C2 · area **FLOW** · from `docs/SURFACE-DESIGN.md`

## Summary
Nodes carry 200+ word capsules that need full width, so the reader is a full-screen overlay (not a cramped split). A tiny one-hop neighbour strip in area colours sits in the header (◦ATT.004 ─┬─▶ ◦ATT.007(here) ◀─── ◦ATT.006) so a node's position in the web reads BEFORE its body.

## Why it matters
Position-before-prose lets the user orient in the graph before committing to 200 words; the strip is the cheapest map of local structure.

## Failure if missing
The capsule opens with no sense of where it sits; the reader becomes a wall of text disconnected from the graph.

## Links
- parents: `FLOW.060`
- children: —
- dependsOn: `NAV.052`
- siblings: `FLOW.062`
- guardedBy: —
