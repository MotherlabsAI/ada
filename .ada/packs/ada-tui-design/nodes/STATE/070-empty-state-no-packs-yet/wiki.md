# STATE.070 · Empty state — no packs yet

> ∵ source · L2 · C3 · area **STATE** · from `src/tui/ink/Welcome.ts`

## Summary
With zero packs the projects panel reads 'no packs yet — Compile an idea' and the default action is Compile. The empty state TEACHES the next step instead of showing a void.

## Why it matters
The first-ever run is the highest-stakes empty state; it must point at the one action that creates value.

## Failure if missing
A new user sees an empty panel and a blinking cursor with no idea that 'Compile an idea' is the way in.

## Links
- parents: `FLOW.060`
- children: —
- dependsOn: —
- siblings: `STATE.071`
- guardedBy: —
