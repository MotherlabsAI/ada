# ROOT.001 · North-star: Claude Code's calm, premium sister

> ∵ source · L4 · C1 · area **ROOT** · from `docs/SURFACE-DESIGN.md`

## Summary
The brief in one line: one accent on calm earth neutrals, one focal point per screen, one moving thing at a time, a frame that never jumps. Terracotta/plum is OURS — match Claude Code's restraint, never copy its hue.

## Why it matters
It is the single tie-breaker for every later decision; when two designs are both 'fine', the calmer one wins.

## Failure if missing
The surface reads as a LangChain-clone dashboard — busy, cool-toned, generic — and the premium promise breaks on first contact.

## Links
- parents: `ROOT.000`
- children: `IDENT.010`, `IDENT.011`, `LAYOUT.034`, `MOTION.040`
- dependsOn: —
- siblings: `ROOT.002`
- guardedBy: —
