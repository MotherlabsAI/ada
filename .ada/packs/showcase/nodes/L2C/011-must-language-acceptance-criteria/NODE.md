# ◇ ● ∴ κ ⇒ L2C.011 — Must-Language -> Acceptance Criteria

- cluster: L2C · depth: L5 · truth: ∴ inference
- checkability: C3 (deterministic)
- compiles to: blueprint, c

**Summary.** 'Must' / 'never' phrases become acceptance criteria the executor builds against.

**Why.** Acceptance criteria turn intent into a testable definition of done.

**Failure if missing.** The build is 'finished' but nobody can say whether it meets intent.

See `wiki.md` for the full article, `edges.yaml` for links, `checkability.yaml` for checks.
