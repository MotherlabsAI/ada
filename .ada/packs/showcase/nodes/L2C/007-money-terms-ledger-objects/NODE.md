# ◇ ● ∴ κ ⇒ L2C.007 — Money Terms -> Ledger Objects

- cluster: L2C · depth: L5 · truth: ∴ inference
- checkability: C5 (static/db)
- compiles to: code, blueprint, c

**Summary.** Money words become ledger objects in integer minor units: deposits, balances, refunds.

**Why.** Floats and ad-hoc money math cause real financial errors.

**Failure if missing.** Rounding drift; negative balances; a refund that exceeds the payment.

See `wiki.md` for the full article, `edges.yaml` for links, `checkability.yaml` for checks.
