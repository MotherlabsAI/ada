# ◌ ● Ω L2C.012 — Unknowns -> Residue

- cluster: L2C · depth: L5 · truth: Ω residue
- checkability: C1 (human review)
- compiles to: wiki

**Summary.** What cannot be answered yet becomes visible residue instead of a fabricated answer.

**Why.** A hole is better than a lie (AXIOM A4).

**Failure if missing.** The system invents a constraint the user never set and builds on it.

See `wiki.md` for the full article, `edges.yaml` for links, `checkability.yaml` for checks.
