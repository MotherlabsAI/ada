# λ ● ∴ ⇒ L2C.013 — Repeated Procedures -> Skills

- cluster: L2C · depth: L5 · truth: ∴ inference
- checkability: C2 (rubric/LLM)
- compiles to: claude

**Summary.** Procedures the operator repeats become Claude Code skills the executor can invoke.

**Why.** Repeated work belongs in a named, reusable procedure, not re-explained each time.

**Failure if missing.** The same instructions get re-typed and drift.

See `wiki.md` for the full article, `edges.yaml` for links, `checkability.yaml` for checks.
