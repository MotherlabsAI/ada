# κ ● ∴ κ L2C.010 — Rules -> Validations

- cluster: L2C · depth: L5 · truth: ∴ inference
- checkability: C4 (property-based)
- compiles to: c

**Summary.** Hard rules become validations: minimum deposit, cancellation window, max party size.

**Why.** Rules stated in prose are not enforced; rules as validations are.

**Failure if missing.** Policy exists on paper but the system happily violates it.

See `wiki.md` for the full article, `edges.yaml` for links, `checkability.yaml` for checks.
