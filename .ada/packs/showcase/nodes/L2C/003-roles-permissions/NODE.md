# ! ● ∴ κ ⇒ L2C.003 — Roles -> Permissions

- cluster: L2C · depth: L5 · truth: ∴ inference
- checkability: C4 (property-based)
- compiles to: code, blueprint, c, gov

**Summary.** Roles become permissions and policies: owner, staff, front-desk, client.

**Why.** Who-can-do-what is a security invariant, not a UI detail.

**Failure if missing.** Privilege bugs: a client cancels another client's booking; staff sees the full ledger.

See `wiki.md` for the full article, `edges.yaml` for links, `checkability.yaml` for checks.
