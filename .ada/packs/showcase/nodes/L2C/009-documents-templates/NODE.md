# ◇ ● ∴ κ ⇒ L2C.009 — Documents -> Templates

- cluster: L2C · depth: L4 · truth: ∴ inference
- checkability: C3 (deterministic)
- compiles to: code, blueprint

**Summary.** Recurring documents become templates: consent forms, quotes, receipts.

**Why.** Templated documents are auditable and consistent; ad-hoc ones are not.

**Failure if missing.** Missing consent on file; inconsistent quote wording.

See `wiki.md` for the full article, `edges.yaml` for links, `checkability.yaml` for checks.
