# ◇ ● ∴ ⇒ L2C.014 — Desired UX -> Routes / Components

- cluster: L2C · depth: L5 · truth: ∴ inference
- checkability: C2 (rubric/LLM)
- compiles to: code, blueprint

**Summary.** Experience language becomes routes and components: a calendar, a lead inbox, a payments view.

**Why.** UX intent must map to concrete screens or it stays a vibe.

**Failure if missing.** The executor guesses the screens and builds the wrong workflow.

See `wiki.md` for the full article, `edges.yaml` for links, `checkability.yaml` for checks.
