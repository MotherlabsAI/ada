# κ ● ∴ κ L2C.016 — Failure Language -> C Candidate

- cluster: L2C · depth: L5 · truth: ∴ inference
- checkability: C4 (property-based)
- compiles to: c

**Summary.** 'It must never X' becomes a candidate deterministic check.

**Why.** Stated failure modes are the richest source of durable invariants.

**Failure if missing.** Known failure modes recur because nothing was written to catch them.

See `wiki.md` for the full article, `edges.yaml` for links, `checkability.yaml` for checks.
