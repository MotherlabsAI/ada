# ◇ ● ∴ κ ⇒ DATA.002 — Database Schema

- cluster: DATA · depth: L5 · truth: ∴ inference
- checkability: C5 (static/db)
- compiles to: code, blueprint, c

**Summary.** The canonical relational schema the executor builds and migrates against.

**Why.** One source of truth for shape; constraints live closest to the data.

**Failure if missing.** Drifting ad-hoc tables that no constraint protects.

See `wiki.md` for the full article, `edges.yaml` for links, `checkability.yaml` for checks.
