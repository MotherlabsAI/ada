# ◇ ● ∴ κ ⇒ DATA.004 — Staff Table

- cluster: DATA · depth: L5 · truth: ∴ inference
- checkability: C5 (static/db)
- compiles to: code, blueprint, c

**Summary.** staff(id, name, role, active).

**Why.** Scopes bookings and availability to a real person.

**Failure if missing.** Bookings cannot be attributed; availability is meaningless.

See `wiki.md` for the full article, `edges.yaml` for links, `checkability.yaml` for checks.
