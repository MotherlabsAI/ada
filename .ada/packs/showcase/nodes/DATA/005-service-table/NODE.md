# ◇ ● ∴ κ ⇒ DATA.005 — Service Table

- cluster: DATA · depth: L4 · truth: ∴ inference
- checkability: C5 (static/db)
- compiles to: code, blueprint, c

**Summary.** services(id, name, minutes, price_cents).

**Why.** Duration and price drive scheduling and the ledger.

**Failure if missing.** No canonical duration; overlap and pricing cannot be computed.

See `wiki.md` for the full article, `edges.yaml` for links, `checkability.yaml` for checks.
