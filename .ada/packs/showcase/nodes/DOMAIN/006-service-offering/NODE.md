# ◇ ● ∴ κ ⇒ DOMAIN.006 — Service Offering

- cluster: DOMAIN · depth: L5 · truth: ∴ inference
- checkability: C3 (deterministic)
- compiles to: code, blueprint

**Summary.** Something bookable, with a duration and a price.

**Why.** Duration drives scheduling; price drives the ledger.

**Failure if missing.** Bookings have no canonical duration, so overlap and capacity cannot be computed.

See `wiki.md` for the full article, `edges.yaml` for links, `checkability.yaml` for checks.
