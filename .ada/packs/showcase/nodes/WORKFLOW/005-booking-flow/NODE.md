# κ ● ∴ κ ⇒ WORKFLOW.005 — Booking Flow

- cluster: WORKFLOW · depth: L5 · truth: ∴ inference
- checkability: C4 (property-based)
- compiles to: blueprint, c

**Summary.** Select service -> choose staff and slot -> confirm -> hold with deposit. The flow the double-booking check guards.

**Why.** The highest-traffic, highest-stakes workflow in the command center.

**Failure if missing.** Two clients hold the same staff slot; the day falls apart at the desk.

See `wiki.md` for the full article, `edges.yaml` for links, `checkability.yaml` for checks.
