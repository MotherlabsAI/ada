# κ ● ∴ κ ⇒ WORKFLOW.008 — Cancellation Flow

- cluster: WORKFLOW · depth: L4 · truth: ∴ inference
- checkability: C4 (property-based)
- compiles to: blueprint, c

**Summary.** Release a slot and apply the cancellation policy to the deposit.

**Why.** Cancellation policy is real money and must be enforced, not implied.

**Failure if missing.** A cancelled booking still blocks the slot or is still charged.

See `wiki.md` for the full article, `edges.yaml` for links, `checkability.yaml` for checks.
