# κ ● ∴ κ ⇒ WORKFLOW.006 — Deposit Flow

- cluster: WORKFLOW · depth: L5 · truth: ∴ inference
- checkability: C5 (static/db)
- compiles to: blueprint, c

**Summary.** Capture a deposit at booking time and reconcile it at checkout.

**Why.** Deposits are money; they must reconcile exactly.

**Failure if missing.** Deposit double-counted or dropped on reschedule.

See `wiki.md` for the full article, `edges.yaml` for links, `checkability.yaml` for checks.
