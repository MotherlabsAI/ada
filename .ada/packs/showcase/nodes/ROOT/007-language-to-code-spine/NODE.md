# ⟡ ● ∴ κ ⇒ ROOT.007 — Language-to-Code Spine

- cluster: ROOT · depth: L5 · truth: ∴ inference
- checkability: C3 (deterministic)
- compiles to: graph, wiki, blueprint, claude, c

**Summary.** The translation layer that turns human language into software primitives: nouns into entities, verbs into actions, rules into checks.

**Why.** Without it, the executor builds around vague prose instead of stable domain objects.

**Failure if missing.** Inconsistent names, duplicate models, weak schemas, UI that does not match the domain.

See `wiki.md` for the full article, `edges.yaml` for links, `checkability.yaml` for checks.
