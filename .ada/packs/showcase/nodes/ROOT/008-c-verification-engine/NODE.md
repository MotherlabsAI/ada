# κ ● ∴ κ ROOT.008 — C Verification Engine

- cluster: ROOT · depth: L5 · truth: ∴ inference
- checkability: C5 (static/db)
- compiles to: c

**Summary.** The deterministic trust layer. Runnable pass/fail predicates that survive model changes because they check outputs directly.

**Why.** Generated code is disposable and prompts go stale; executable invariants are the compounding trust asset.

**Failure if missing.** Wrong output looks coherent and ships, because nothing checked it.

See `wiki.md` for the full article, `edges.yaml` for links, `checkability.yaml` for checks.
