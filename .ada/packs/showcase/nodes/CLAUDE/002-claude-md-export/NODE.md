# λ ● ∴ ⇒ CLAUDE.002 — CLAUDE.md Export

- cluster: CLAUDE · depth: L5 · truth: ∴ inference
- checkability: C2 (rubric/LLM)
- compiles to: claude

**Summary.** Project instructions telling Claude Code which pack to load, what matters, what not to touch, and when to ask.

**Why.** The executor's entry point into the compiled world.

**Failure if missing.** Claude builds from a raw prompt with no governed context.

See `wiki.md` for the full article, `edges.yaml` for links, `checkability.yaml` for checks.
