# BUILD.md
> Generated by Ada BLD stage. Do not edit manually — re-run `ada compile --amend` to update.

## Stack
**Next.js + Prisma + PostgreSQL**
> All implementation decisions must use this stack. Do not introduce alternative frameworks.

## Acceptance Criteria
The build is complete when all of the following hold:

- **compilation-pipeline:** Done when compilationRun.status == COMPLETE; blueprintPostcode recorded; Manifest persists with all 6 artifact postcodes; .ada/state.json reflects completed run; all ProvenanceRecords form an unbroken chain from draftIntentGraph.postcode to blueprintPostcode; CompilationRun.completedAt is set
- **provenance:** Done when compilationRun.status == COMPLETE; blueprintPostcode recorded; Manifest persists with all 6 artifact postcodes; .ada/state.json reflects completed run; all ProvenanceRecords form an unbroken chain from draftIntentGraph.postcode to blueprintPostcode; CompilationRun.completedAt is set
- **runtime-governance:** Done when session.status == CLOSED; Session.endedAt is set; .ada/state.json reflects final WorldState; activeDelegations is empty; all DelegationContracts referenced in session are in terminal state; AuditReport exists for session; WorldState.sessionCount == prior sessionCount + 1
- **self-improvement:** Done when compilationRun.status == COMPLETE; blueprintPostcode recorded; Manifest persists with all 6 artifact postcodes; .ada/state.json reflects completed run; all ProvenanceRecords form an unbroken chain from draftIntentGraph.postcode to blueprintPostcode; CompilationRun.completedAt is set
- **elicitation:** Done when compilationRun.status == COMPLETE; blueprintPostcode recorded; Manifest persists with all 6 artifact postcodes; .ada/state.json reflects completed run; all ProvenanceRecords form an unbroken chain from draftIntentGraph.postcode to blueprintPostcode; CompilationRun.completedAt is set
- **artifact-output:** Done when compilationRun.status == COMPLETE; blueprintPostcode recorded; Manifest persists with all 6 artifact postcodes; .ada/state.json reflects completed run; all ProvenanceRecords form an unbroken chain from draftIntentGraph.postcode to blueprintPostcode; CompilationRun.completedAt is set
- **storage:** Done when compilationRun.status == COMPLETE; blueprintPostcode recorded; Manifest persists with all 6 artifact postcodes; .ada/state.json reflects completed run; all ProvenanceRecords form an unbroken chain from draftIntentGraph.postcode to blueprintPostcode; CompilationRun.completedAt is set

## File Tree
Create exactly these files. Do not create files outside this tree without explicit instruction.

```
  artifact-output/  # artifact-output bounded context
  compilation-pipeline/  # compilation-pipeline bounded context
  elicitation/  # elicitation bounded context
  provenance/  # provenance bounded context
  runtime-governance/  # runtime-governance bounded context
  self-improvement/  # self-improvement bounded context
  storage/  # storage bounded context
        route.ts  # Application entry point
    layout.tsx  # Application entry point
    page.tsx  # Application entry point
    blueprint-to-claudemd.test.ts  # Tests for blueprintToCLAUDEMD ← blueprintToCLAUDEMD
    blueprint-to-claudemd.ts  # Transforms Blueprint into CLAUDE ← blueprintToCLAUDEMD
    components-to-agents.test.ts  # Tests for componentsToAgents ← componentsToAgents
    components-to-agents.ts  # Transforms Blueprint components into AgentFile markdown governance documents with model, fileScope,  ← componentsToAgents
    derive-build-contract.test.ts  # Tests for deriveBuildContract ← deriveBuildContract
    derive-build-contract.ts  # Derives BuildContract with file tree, acceptance criteria, and gate pass from GOV ACCEPT decision ← deriveBuildContract
    index.ts  # Public API for artifact-output bounded context
    invariants-to-hooks.test.ts  # Tests for invariantsToHooks ← invariantsToHooks
    invariants-to-hooks.ts  # Transforms Blueprint invariants into HookScript pre-tool and post-tool enforcement scripts encoding  ← invariantsToHooks
    write-config-graph.test.ts  # Tests for writeConfigGraph ← writeConfigGraph
    write-config-graph.ts  # Writes the complete ConfigGraph output — CLAUDE ← writeConfigGraph
    analyze-codebase.test.ts  # Tests for analyzeCodebase ← analyzeCodebase
    analyze-codebase.ts  # Deterministic CTX stage component (C16) that scans the target project codebase to produce CodebaseCo ← analyzeCodebase
    confidence-tracker.test.ts  # Tests for ConfidenceTracker ← ConfidenceTracker
    confidence-tracker.ts  # Tracks confidence scores across pipeline iterations and computes aggregate confidence for GovernorDe ← ConfidenceTracker
    diff-blueprint-against-code.test.ts  # Tests for diffBlueprintAgainstCode ← diffBlueprintAgainstCode
    diff-blueprint-against-code.ts  # Computes delta between existing code state and target blueprint state, enabling recompilation awaren ← diffBlueprintAgainstCode
    entity-agent.test.ts  # Tests for EntityAgent ← EntityAgent
    entity-agent.ts  # Models bounded contexts, entities, invariants, and properties from the enriched intent ← EntityAgent
    entity-extractor.test.ts  # Tests for EntityExtractor ← EntityExtractor
    entity-extractor.ts  # Extracts entities from enriched intent using LLM-backed analysis ← EntityExtractor
    fallback-blueprint-result.test.ts  # Tests for FallbackBlueprintResult ← FallbackBlueprintResult
    fallback-blueprint-result.ts  # Records terminal failure state when 3 consecutive GOV REJECTs exhaust the iteration budget (G12) ← FallbackBlueprintResult
    governor-agent.test.ts  # Tests for GovernorAgent ← GovernorAgent
    governor-agent.ts  # Evaluates full pipeline state and issues ACCEPT/REJECT/ITERATE GovernorDecision with confidence scor ← GovernorAgent
    index.ts  # Public API for compilation-pipeline bounded context
    intent-agent.test.ts  # Tests for IntentAgent ← IntentAgent
    intent-agent.ts  # Parses raw intent into a structured IntentGraph with goals (stated, derived, implied, unstated), con ← IntentAgent
    intstage-controller.test.ts  # Tests for INTStageController ← INTStageController
    intstage-controller.ts  # Controls INT stage rerun pipeline when GOV issues ITERATE decision ← INTStageController
    mother-compiler.test.ts  # Tests for MotherCompiler ← MotherCompiler
    mother-compiler.ts  # Coordinates individual stage agents (IntentAgent, PersonaAgent, EntityAgent, ProcessAgent, Synthesis ← MotherCompiler
    persona-agent.test.ts  # Tests for PersonaAgent ← PersonaAgent
    persona-agent.ts  # Enriches the IntentGraph with domain context, stakeholder identification, and excluded concerns ← PersonaAgent
    pipeline-orchestrator.test.ts  # Tests for PipelineOrchestrator ← PipelineOrchestrator
    pipeline-orchestrator.ts  # Orchestrates the 9-stage sequential compilation pipeline (CTX→INT→PER→ENT→PRO→SYN→VER→GOV→BLD), mana ← PipelineOrchestrator
    process-agent.test.ts  # Tests for ProcessAgent ← ProcessAgent
    process-agent.ts  # Derives workflows, state machines, and process constraints from the entity model ← ProcessAgent
    syngate-evaluator.test.ts  # Tests for SYNGateEvaluator ← SYNGateEvaluator
    syngate-evaluator.ts  # Evaluates the SYN stage gate — validates that synthesis output meets entropy reduction thresholds an ← SYNGateEvaluator
    synthesis-agent.test.ts  # Tests for SynthesisAgent ← SynthesisAgent
    synthesis-agent.ts  # Integrates entity model, process model, and constraints into a unified Blueprint draft ← SynthesisAgent
    verify-agent.test.ts  # Tests for VerifyAgent ← VerifyAgent
    verify-agent.ts  # Runs the 5-layer verification stack (structural, execution, policy, outcome, provenance) against the ← VerifyAgent
    workspace-package-scanner.test.ts  # Tests for WorkspacePackageScanner ← WorkspacePackageScanner
    workspace-package-scanner.ts  # Scans pnpm workspace packages (C2) to discover package boundaries and dependencies ← WorkspacePackageScanner
    dialogue-engine.test.ts  # Tests for DialogueEngine ← DialogueEngine
    dialogue-engine.ts  # Drives proposals-first elicitation dialogue — Ada proposes structure, user confirms ← DialogueEngine
    draft-intent-graph-manager.test.ts  # Tests for DraftIntentGraphManager ← DraftIntentGraphManager
    draft-intent-graph-manager.ts  # Maintains the evolving DraftIntentGraph during elicitation — the handoff artifact (G10) ← DraftIntentGraphManager
    elicitation-session-manager.test.ts  # Tests for ElicitationSessionManager ← ElicitationSessionManager
    elicitation-session-manager.ts  # Manages ElicitationSession lifecycle (OPEN→ELICITING→GAP_ANALYSIS→READY→HANDED_OFF), orchestrates di ← ElicitationSessionManager
    gap-analyzer.test.ts  # Tests for GapAnalyzer ← GapAnalyzer
    gap-analyzer.ts  # Detects blocking unknowns and gaps in the draft intent graph ← GapAnalyzer
    handoff-emitter.test.ts  # Tests for HandoffEmitter ← HandoffEmitter
    handoff-emitter.ts  # Emits HandoffRecord when ElicitationSession reaches READY state, creating the draftIntentGraph→INT p ← HandoffEmitter
    index.ts  # Public API for elicitation bounded context
    readiness-assessor.test.ts  # Tests for ReadinessAssessor ← ReadinessAssessor
    readiness-assessor.ts  # Evaluates whether ElicitationSession is compilation-ready (all blocking gaps resolved) ← ReadinessAssessor
    entgate-evaluator.test.ts  # Tests for ENTGateEvaluator ← ENTGateEvaluator
    entgate-evaluator.ts  # Evaluates the ENT gate — checks provenance integrity and all blockers cleared before allowing ENT st ← ENTGateEvaluator
    index.ts  # Public API for provenance bounded context
    postcode-address-factory.test.ts  # Tests for PostcodeAddressFactory ← PostcodeAddressFactory
    postcode-address-factory.ts  # Creates content-addressed PostcodeAddress instances with ML- prefix, 8-character hash derived from c ← PostcodeAddressFactory
    provenance-chain-validator.test.ts  # Tests for ProvenanceChainValidator ← ProvenanceChainValidator
    provenance-chain-validator.ts  # Validates ProvenanceChain integrity including 3-hop constraint for ENT artifacts, ensures fromPostco ← ProvenanceChainValidator
    provenance-record-writer.test.ts  # Tests for ProvenanceRecordWriter ← ProvenanceRecordWriter
    provenance-record-writer.ts  # Writes provenance records at gate passage points (post-stage, per C5) ← ProvenanceRecordWriter
    provenance-store.test.ts  # Tests for ProvenanceStore ← ProvenanceStore
    provenance-store.ts  # Persists and retrieves ProvenanceRecord instances ← ProvenanceStore
    build-world-state.test.ts  # Tests for buildWorldState ← buildWorldState
    build-world-state.ts  # Initializes and maintains WorldState with StateFact entries, uncertainty scoring, and confidence-bas ← buildWorldState
    delegation-contract.test.ts  # Tests for DelegationContract ← DelegationContract
    delegation-contract.ts  # Typed contract binding a micro-executor to a specific fileScope, maxRecursionDepth, stop conditions, ← DelegationContract
    index.ts  # Public API for runtime-governance bounded context
    macro-plan.test.ts  # Tests for MacroPlan ← MacroPlan
    macro-plan.ts  # Represents the macro-level execution plan with ordered tasks respecting component dependency graph ← MacroPlan
    run-compile-loop.test.ts  # Tests for runCompileLoop ← runCompileLoop
    run-compile-loop.ts  # Executes the governed agent execution session — compiles delegation contracts, runs domain agent tas ← runCompileLoop
    start-server.test.ts  # Tests for startServer ← startServer
    start-server.ts  # Starts the stdio MCP server as a subprocess (C10) ← startServer
    verification-report.test.ts  # Tests for VerificationReport ← VerificationReport
    verification-report.ts  # Aggregates verification results across 5 layers — entity coverage, invariant coverage, structural ch ← VerificationReport
    write-checkpoint.test.ts  # Tests for writeCheckpoint ← writeCheckpoint
    write-checkpoint.ts  # Creates RuntimeCheckpoint snapshots of world state for crash recovery and session resume ← writeCheckpoint
    amendment.test.ts  # Tests for Amendment ← Amendment
    amendment.ts  # Represents a proposed change to the blueprint, exclusively human-approved (C8) ← Amendment
    index.ts  # Public API for self-improvement bounded context
    skill-candidate.test.ts  # Tests for SkillCandidate ← SkillCandidate
    skill-candidate.ts  # Tracks candidate patterns observed across sessions before promotion to Skill ← SkillCandidate
    skill.test.ts  # Tests for Skill ← Skill
    skill.ts  # Represents an extracted execution pattern from ≥ 2 source sessions, promoted to approved status with ← Skill
    ada-storage.test.ts  # Tests for AdaStorage ← AdaStorage
    ada-storage.ts  # Manages SQLite persistence for RunRecord and ProjectRecord ← AdaStorage
    elicitation-store.test.ts  # Tests for ElicitationStore ← ElicitationStore
    elicitation-store.ts  # Persists ElicitationSession state, turns, and draft intent graphs ← ElicitationStore
    index.ts  # Public API for storage bounded context
  migrations/  # Database migration history
  schema.prisma  # Database schema — entities map to models here
tsconfig.json  # TypeScript configuration
package.json  # Project manifest and scripts
.env.example  # Required environment variables (template)
```

## Dependencies
Install exactly these packages. Do not add unlisted packages.

**dependencies:**
- `@prisma/client`
- `bcrypt`
- `multer`
- `next`
- `pino`
- `react`
- `react-dom`
- `zod`

**devDependencies:**
- `@types/bcrypt`
- `@types/node`
- `@types/react`
- `@types/react-dom`
- `@vitejs/plugin-react`
- `prisma`
- `typescript`
- `vitest`

## Provenance
Stack: `nextjs-prisma-postgres`  
Gate: PASS  
Postcode: `ML.BLD.7a643406/v1`