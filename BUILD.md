# BUILD.md
> Generated by Ada BLD stage. Do not edit manually — re-run `ada compile --amend` to update.

## Stack
**Node.js CLI**
> All implementation decisions must use this stack. Do not introduce alternative frameworks.

## Acceptance Criteria
The build is complete when all of the following hold:

- **Bootstrap:** Done when all artifacts with changed postcodes written to filesystem; artifacts with unchanged postcodes untouched; WorldModel checkpointed in .ada/world-model.json; SessionReloadSignal emitted and acknowledged by consumer; every written artifact has embedded compilationRunId and postcode in header
- **GovernanceCore:** Done when exactly one outcome: tool execution allowed with no payload, or GateFailurePayload emitted with tool execution blocked; GateFailurePayload contains all eight required fields if emitted; ContinuousGovernor activationLog updated with this gate event; verdict is irrevocable for this PreToolUse event
- **GateEnforcement:** Done when all artifacts with changed postcodes written to filesystem; artifacts with unchanged postcodes untouched; WorldModel checkpointed in .ada/world-model.json; SessionReloadSignal emitted and acknowledged by consumer; every written artifact has embedded compilationRunId and postcode in header
- **IntentResolution:** Done when all artifacts with changed postcodes written to filesystem; artifacts with unchanged postcodes untouched; WorldModel checkpointed in .ada/world-model.json; SessionReloadSignal emitted and acknowledged by consumer; every written artifact has embedded compilationRunId and postcode in header
- **MCPIntegration:** Done when all artifacts with changed postcodes written to filesystem; artifacts with unchanged postcodes untouched; WorldModel checkpointed in .ada/world-model.json; SessionReloadSignal emitted and acknowledged by consumer; every written artifact has embedded compilationRunId and postcode in header
- **WorldModeling:** Done when all artifacts with changed postcodes written to filesystem; artifacts with unchanged postcodes untouched; WorldModel checkpointed in .ada/world-model.json; SessionReloadSignal emitted and acknowledged by consumer; every written artifact has embedded compilationRunId and postcode in header
- **PipelineOrchestration:** Done when all artifacts with changed postcodes written to filesystem; artifacts with unchanged postcodes untouched; WorldModel checkpointed in .ada/world-model.json; SessionReloadSignal emitted and acknowledged by consumer; every written artifact has embedded compilationRunId and postcode in header
- **DriftMonitoring:** Done when all artifacts with changed postcodes written to filesystem; artifacts with unchanged postcodes untouched; WorldModel checkpointed in .ada/world-model.json; SessionReloadSignal emitted and acknowledged by consumer; every written artifact has embedded compilationRunId and postcode in header
- **ArtifactProvenance:** Done when all artifacts with changed postcodes written to filesystem; artifacts with unchanged postcodes untouched; WorldModel checkpointed in .ada/world-model.json; SessionReloadSignal emitted and acknowledged by consumer; every written artifact has embedded compilationRunId and postcode in header

## File Tree
Create exactly these files. Do not create files outside this tree without explicit instruction.

```
  artifactprovenance/  # ArtifactProvenance bounded context
  bootstrap/  # Bootstrap bounded context
  driftmonitoring/  # DriftMonitoring bounded context
  gateenforcement/  # GateEnforcement bounded context
  governancecore/  # GovernanceCore bounded context
  intentresolution/  # IntentResolution bounded context
  mcpintegration/  # MCPIntegration bounded context
  pipelineorchestration/  # PipelineOrchestration bounded context
  worldmodeling/  # WorldModeling bounded context
    blueprint-to-claudemd.test.ts  # Tests for blueprintToCLAUDEMD ← blueprintToCLAUDEMD
    blueprint-to-claudemd.ts  # Generates CLAUDE ← blueprintToCLAUDEMD
    blueprint-to-contracts.test.ts  # Tests for blueprintToContracts ← blueprintToContracts
    blueprint-to-contracts.ts  # Generates delegation contract files from blueprint ← blueprintToContracts
    build-settings.test.ts  # Tests for buildSettings ← buildSettings
    build-settings.ts  # Generates settings ← buildSettings
    components-to-agents.test.ts  # Tests for componentsToAgents ← componentsToAgents
    components-to-agents.ts  # Generates agent definition files from blueprint components ← componentsToAgents
    index.ts  # Public API for ArtifactProvenance bounded context
    invariants-to-hooks.test.ts  # Tests for invariantsToHooks ← invariantsToHooks
    invariants-to-hooks.ts  # Generates invariant enforcement hook scripts from blueprint invariants ← invariantsToHooks
    manifold-projector.test.ts  # Tests for ManifoldProjector ← ManifoldProjector
    manifold-projector.ts  # Projects blueprint state into the artifact space, computing which artifacts need regeneration by com ← ManifoldProjector
    provenance-chain-validator.test.ts  # Tests for ProvenanceChainValidator ← ProvenanceChainValidator
    provenance-chain-validator.ts  # Validates provenance chain integrity across pipeline stages, ensuring each artifact traces back to i ← ProvenanceChainValidator
    workflows-to-skills.test.ts  # Tests for workflowsToSkills ← workflowsToSkills
    workflows-to-skills.ts  # Generates skill files from blueprint workflows ← workflowsToSkills
    ada-storage.test.ts  # Tests for AdaStorage ← AdaStorage
    ada-storage.ts  # File-based persistence layer for all Ada artifacts, project records, and run records ← AdaStorage
    bootstrap-seed.test.ts  # Tests for BootstrapSeed ← BootstrapSeed
    bootstrap-seed.ts  # Holds immutable meta-invariants and the PreToolUse write-block hook as a static artifact checked int ← BootstrapSeed
    index.ts  # Public API for Bootstrap bounded context
    provenance-store.test.ts  # Tests for ProvenanceStore ← ProvenanceStore
    provenance-store.ts  # Low-level provenance persistence including postcode generation, parsing, and validation ← ProvenanceStore
  cli.ts  # Application entry point
    drift-score-calculator.test.ts  # Tests for DriftScoreCalculator ← DriftScoreCalculator
    drift-score-calculator.ts  # Computes normalized drift scores using weighted formula: score = w1*semanticDivergence + w2*blueprin ← DriftScoreCalculator
    embedding-provider.test.ts  # Tests for EmbeddingProvider ← EmbeddingProvider
    embedding-provider.ts  # Singleton shared component that provides embedding inference consumed by SemanticGate, DriftDetector ← EmbeddingProvider
    evaluate-semantic-drift.test.ts  # Tests for evaluateSemanticDrift ← evaluateSemanticDrift
    evaluate-semantic-drift.ts  # Evaluates semantic drift by comparing current session state against the reference blueprint, using t ← evaluateSemanticDrift
    index.ts  # Public API for DriftMonitoring bounded context
    create-governed-can-use-tool.test.ts  # Tests for createGovernedCanUseTool ← createGovernedCanUseTool
    create-governed-can-use-tool.ts  # Factory function that creates the PreToolUse hook handler wiring the 30-second soft ceiling timeout  ← createGovernedCanUseTool
    entgate-evaluator.test.ts  # Tests for ENTGateEvaluator ← ENTGateEvaluator
    entgate-evaluator.ts  # Evaluates the ENT gate pass conditions: entity count > 0, provenance intact, all blockers cleared, W ← ENTGateEvaluator
    evaluate-semantic-gate.test.ts  # Tests for evaluateSemanticGate ← evaluateSemanticGate
    evaluate-semantic-gate.ts  # Core gate enforcement function that classifies tool invocations against actor classes (injection/sco ← evaluateSemanticGate
    index.ts  # Public API for GateEnforcement bounded context
    confidence-tracker.test.ts  # Tests for ConfidenceTracker ← ConfidenceTracker
    confidence-tracker.ts  # Tracks confidence scores across governor iterations to detect convergence or divergence, informing w ← ConfidenceTracker
    governor.test.ts  # Tests for Governor ← Governor
    governor.ts  # Implements ContinuousGovernor behavior: activates on PreToolUse events, every 50 assistant output to ← Governor
    index.ts  # Public API for GovernanceCore bounded context
  index.ts  # Application entry point
    dialogue-engine.test.ts  # Tests for DialogueEngine ← DialogueEngine
    dialogue-engine.ts  # Manages the elicitation dialogue for intent clarification when the INT stage detects ambiguities tha ← DialogueEngine
    ground-intent.test.ts  # Tests for groundIntent ← groundIntent
    ground-intent.ts  # Performs CTX stage context extraction — discovers project context from codebase analysis and grounds ← groundIntent
    index.ts  # Public API for IntentResolution bounded context
    intstage-controller.test.ts  # Tests for INTStageController ← INTStageController
    intstage-controller.ts  # Controls the INT stage — resolves grounded intent into a structured intent graph with goals, constra ← INTStageController
    check-drift.test.ts  # Tests for checkDrift ← checkDrift
    check-drift.ts  # MCP tool handler that computes and returns the current drift score ← checkDrift
    compile-intent.test.ts  # Tests for compileIntent ← compileIntent
    compile-intent.ts  # MCP tool handler that receives raw natural language intent and optional compilationRunId, delegating ← compileIntent
    index.ts  # Public API for MCPIntegration bounded context
    start-server.test.ts  # Tests for startServer ← startServer
    start-server.ts  # Initializes and runs the MCP server over stdio/JSON-RPC transport, registering all 12 required tools ← startServer
    index.ts  # Public API for PipelineOrchestration bounded context
    mother-compiler.test.ts  # Tests for MotherCompiler ← MotherCompiler
    mother-compiler.ts  # Top-level compiler coordination that wires pipeline stages to their handlers, manages compilation ru ← MotherCompiler
    persona-agent.test.ts  # Tests for PersonaAgent ← PersonaAgent
    persona-agent.ts  # Concrete non-stub PersonaResolver handler for the PER stage ← PersonaAgent
    pipeline-orchestrator.test.ts  # Tests for PipelineOrchestrator ← PipelineOrchestrator
    pipeline-orchestrator.ts  # Orchestrates the 9-stage sequential pipeline execution, enforcing strict ordering (CTX→INT→PER→ENT→P ← PipelineOrchestrator
    process-agent.test.ts  # Tests for ProcessAgent ← ProcessAgent
    process-agent.ts  # Concrete non-stub ProcessModeler handler for the PRO stage ← ProcessAgent
    run-store.test.ts  # Tests for RunStore ← RunStore
    run-store.ts  # Manages compilation run records including compilationRunId generation and run state persistence ← RunStore
    build-world-model.test.ts  # Tests for buildWorldModel ← buildWorldModel
    build-world-model.ts  # Constructs and persists the WorldModel typed graph to  ← buildWorldModel
    index.ts  # Public API for WorldModeling bounded context
    write-config-graph.test.ts  # Tests for writeConfigGraph ← writeConfigGraph
    write-config-graph.ts  # Serializes the world model and associated configuration to the  ← writeConfigGraph
tsconfig.json  # TypeScript configuration
package.json  # Project manifest and scripts
.env.example  # Required environment variables (template)
```

## Dependencies
Install exactly these packages. Do not add unlisted packages.

**dependencies:**
- `chalk`
- `commander`
- `conf`
- `consola`
- `fs-extra`
- `zod`

**devDependencies:**
- `@types/node`
- `tsx`
- `typescript`
- `vitest`

## Provenance
Stack: `cli-node`  
Gate: PASS  
Postcode: `ML.L4A.CFG.GLO.HOW.SFT.da4313a9/v1`