# BUILD.md

> Generated by Ada BLD stage. Updated to reflect actual TypeScript monorepo implementation.

## Stack

**TypeScript monorepo packages (pnpm workspace + tsc composite)**

> Three pure TypeScript packages with no runtime framework dependencies. Each package uses `tsc` with `composite: true` and project references.

## Acceptance Criteria

The build is complete when all of the following hold:

- **Workspace:** Done when `buildOrder.hasCycle` is false; `BuildOrder.orderedPackages` contains all `WorkspacePackage` names exactly once; for every `PackageDependencyEdge` (A→B), B appears before A in `BuildOrder.orderedPackages`
- **CompilationConfiguration:** Done when all `Entrypoint.compiledJsPath` files exist and are non-empty; `Entrypoint.mainFieldValue` correctly points to compiled output; cli entrypoint has executable bit set; `Entrypoint` state is verified for mcp-server and cli packages
- **Artifact:** Done when all `Entrypoint.compiledJsPath` files exist and are non-empty; `Entrypoint.mainFieldValue` correctly points to compiled output; cli entrypoint has executable bit set; `Entrypoint` state is verified for mcp-server and cli packages

## File Tree

```
packages/workspace/
  src/
    types.ts                        # WorkspaceRoot, WorkspacePackage, PackageDependencyEdge, BuildOrder
    workspace-package-scanner.ts    # Discovers all workspace packages from pnpm-workspace.yaml
    validate-dependency-graph.ts    # Cycle detection (DFS) + Kahn's topo sort
    topo-waves.ts                   # Groups ordered packages into parallel build waves
    index.ts                        # Public API for Workspace bounded context
  package.json
  tsconfig.json

packages/compilation-configuration/
  src/
    types.ts                        # TsConfig, ProjectReference, CompositeBuild, BuildResult, TsBuildInfo
    derive-build-contract.ts        # Reads and validates tsconfig.json composite contracts
    composite-build-executor.ts     # Invokes tsc -b, parses errors, cleans artifacts, reads tsbuildinfo
    index.ts                        # Public API for CompilationConfiguration bounded context
  package.json
  tsconfig.json

packages/artifact/
  src/
    types.ts                        # DistArtifact, DeclarationFile, Entrypoint, VerificationResult
    verify.ts                       # Verifies dist/ output + primary entrypoints (mcp-server, cli)
    index.ts                        # Public API for Artifact bounded context
  package.json
  tsconfig.json
```

## Verification Results (last run)

- Packages discovered: 23 (from pnpm-workspace.yaml)
- Dependency edges: 44
- Cycle detected: false
- Build waves: 7
- Packages passing artifact check: 23/23
- MCP server entrypoint (`@ada/mcp-server`): VERIFIED
- CLI entrypoint (`@motherlabs/ada`): VERIFIED (executable bit set)

## Provenance

Stack: `typescript-monorepo-packages`
Gate: PASS
Postcode: `ML.L4A.CFG.GLO.HOW.SFT.0189c731/v1`
