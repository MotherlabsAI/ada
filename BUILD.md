# BUILD.md
> Generated by Ada BLD stage. Do not edit manually — re-run `ada compile --amend` to update.

## Stack
**Node.js CLI**
> All implementation decisions must use this stack. Do not introduce alternative frameworks.

## Acceptance Criteria
The build is complete when all of the following hold:

- **PipelineExecution:** Done when compileResult.status = 'success', CompilationRun.state = 'succeeded', CompilationRun.completedAt is set, PipelineState.pipelineState = 'success', GovernorDecision is non-null and references this CompilationRun, zero TypeScript errors across all 8 packages
- **ENTGate:** Done when compileResult.status = 'success', CompilationRun.state = 'succeeded', CompilationRun.completedAt is set, PipelineState.pipelineState = 'success', GovernorDecision is non-null and references this CompilationRun, zero TypeScript errors across all 8 packages
- **BlueprintRegistry:** Done when compileResult.status = 'success', CompilationRun.state = 'succeeded', CompilationRun.completedAt is set, PipelineState.pipelineState = 'success', GovernorDecision is non-null and references this CompilationRun, zero TypeScript errors across all 8 packages
- **ProvenanceAudit:** Done when compileResult.status = 'success', CompilationRun.state = 'succeeded', CompilationRun.completedAt is set, PipelineState.pipelineState = 'success', GovernorDecision is non-null and references this CompilationRun, zero TypeScript errors across all 8 packages
- **ComponentPackageBinding:** Done when compileResult.status = 'success', CompilationRun.state = 'succeeded', CompilationRun.completedAt is set, PipelineState.pipelineState = 'success', GovernorDecision is non-null and references this CompilationRun, zero TypeScript errors across all 8 packages
- **EntityExtraction:** Done when compileResult.status = 'success', CompilationRun.state = 'succeeded', CompilationRun.completedAt is set, PipelineState.pipelineState = 'success', GovernorDecision is non-null and references this CompilationRun, zero TypeScript errors across all 8 packages
- **WorkspacePackages:** Done when compileResult.status = 'success', CompilationRun.state = 'succeeded', CompilationRun.completedAt is set, PipelineState.pipelineState = 'success', GovernorDecision is non-null and references this CompilationRun, zero TypeScript errors across all 8 packages

## File Tree
Create exactly these files. Do not create files outside this tree without explicit instruction.

```
  blueprintregistry/  # BlueprintRegistry bounded context
  componentpackagebinding/  # ComponentPackageBinding bounded context
  entgate/  # ENTGate bounded context
  entityextraction/  # EntityExtraction bounded context
  pipelineexecution/  # PipelineExecution bounded context
  provenanceaudit/  # ProvenanceAudit bounded context
  workspacepackages/  # WorkspacePackages bounded context
    blueprint-registry-loader.test.ts  # Tests for BlueprintRegistryLoader ← BlueprintRegistryLoader
    blueprint-registry-loader.ts  # Loads and validates the BlueprintComponentRegistry containing exactly 10 NamedBlueprintComponents ← BlueprintRegistryLoader
    index.ts  # Public API for BlueprintRegistry bounded context
  cli.ts  # Application entry point
    c3gap-collapse-resolver.test.ts  # Tests for C3GapCollapseResolver ← C3GapCollapseResolver
    c3gap-collapse-resolver.ts  # Detects the C3AssignmentGap at ordinal-3, resolves it via collapse strategy (not reassignment), and  ← C3GapCollapseResolver
    index.ts  # Public API for ComponentPackageBinding bounded context
    entgate-evaluator.test.ts  # Tests for ENTGateEvaluator ← ENTGateEvaluator
    entgate-evaluator.ts  # Evaluates the ENT gate to determine whether pipeline run ML ← ENTGateEvaluator
    index.ts  # Public API for ENTGate bounded context
    entity-extractor.test.ts  # Tests for EntityExtractor ← EntityExtractor
    entity-extractor.ts  # Extracts CanonicalEntity instances from NamedBlueprintComponents and assembles them into a populated ← EntityExtractor
    index.ts  # Public API for EntityExtraction bounded context
  index.ts  # Application entry point
    index.ts  # Public API for PipelineExecution bounded context
    pipeline-orchestrator.test.ts  # Tests for PipelineOrchestrator ← PipelineOrchestrator
    pipeline-orchestrator.ts  # Coordinates the 7-stage ada-semantic-compilation-pipeline workflow: initializes CompilationRun, sequ ← PipelineOrchestrator
    index.ts  # Public API for ProvenanceAudit bounded context
    provenance-chain-validator.test.ts  # Tests for ProvenanceChainValidator ← ProvenanceChainValidator
    provenance-chain-validator.ts  # Validates that every ProvenanceChainRecord contains exactly 3 ProvenanceChainHops (C7 invariant) ← ProvenanceChainValidator
    index.ts  # Public API for WorkspacePackages bounded context
    workspace-package-scanner.test.ts  # Tests for WorkspacePackageScanner ← WorkspacePackageScanner
    workspace-package-scanner.ts  # Scans the pnpm monorepo workspace structure to discover and model WorkspacePackageNode instances ← WorkspacePackageScanner
tsconfig.json  # TypeScript configuration
package.json  # Project manifest and scripts
.env.example  # Required environment variables (template)
```

## Dependencies
Install exactly these packages. Do not add unlisted packages.

**dependencies:**
- `chalk`
- `commander`
- `consola`
- `fs-extra`
- `zod`

**devDependencies:**
- `@types/node`
- `tsx`
- `typescript`
- `vitest`

## Provenance
Stack: `cli-node`  
Gate: PASS  
Postcode: `ML.BLD.30f42c67/v1`